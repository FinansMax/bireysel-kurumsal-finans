# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# prisma
prisma/*.db

# local/test e-posta çıktısı (bkz. src/lib/auth/email.ts) — gerçek e-posta sağlayıcısı
# entegre edilene kadar kullanılan, disposable test verisi içeren dizin.
/.test-outbox/

# local/test davet e-postası çıktısı (bkz. src/lib/tenants/invitation-email.ts) — aynı gerekçe.
/.test-outbox-invitations/
/.test-outbox-verifications/

# AuditLog arşivi (Issue #188). Silinmeden önce dışa aktarılan denetim kayıtları KİŞİSEL VERİ
# içerir; repository'ye asla girmemelidir. Production'da bu dizin soğuk depolamaya taşınır (#185).
/.audit-archive/

# Disa aktarma ve arsiv ciktilari (Issue #188, #194) - uretilen dosyalar repoya girmez
.data/
